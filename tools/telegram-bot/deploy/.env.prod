# Template only — real values come from saas-workspace/.secrets/telegram-bot.env
# via install.sh (it rewrites POSTS_ROOT and STATE_DB for VPS paths).
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_USER_ID=
TELEGRAM_CHANNEL_ID=
POSTS_ROOT=/opt/unilist-bot/posts
STATE_DB=/opt/unilist-bot/state.db
TZ=Europe/Moscow
