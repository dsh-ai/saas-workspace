# Рабочая память проекта

## Текущий статус

Сформирован первый продуктовый дизайн — фича "Участники опросника".

## Принятые решения

### Survey Participants (2026-03-26)
- Обнаружение новых участников через `sessionToken` в localStorage (не требует логина)
- Интерстишл "Кто вы?" показывается только при повторном открытии ссылки с нового устройства
- Минимальные данные от нового участника: Имя (обязательно), Должность + Email (опционально)
- Кнопка "Делегировать" = просто шеринг ссылки, без попапа
- Режим редактирования: последовательный (Base), одновременный (Pro — не реализован)
- CRM sync: создать контакт → привязать к сделке → запись в таймлайн

## Известные проблемы

(не заполнено)

## TODO

- Реализация фичи Survey Participants (design готов)
- Одновременное редактирование (Pro-тариф) — отдельный скоуп

## Контекст окружения

- Код: `/Users/shuvaev/Продукты/unilist/dev_unilist/`
- CRM: Bitrix24 (интеграция уже есть в `events.gateway.ts`)
- Опросники: `Survey` entity с `clientName`/`clientEmail`, уникальная ссылка `/form/{uuid}`
